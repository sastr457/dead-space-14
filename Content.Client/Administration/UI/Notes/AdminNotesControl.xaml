<Control xmlns="https://spacestation14.io">
    <!-- DS14-start -->
    <PanelContainer StyleClasses="DS14MenuPanelDark">
        <BoxContainer Orientation="Vertical"
                      SeparationOverride="8">
            <PanelContainer StyleClasses="DS14MenuInset"
                            VerticalExpand="True"
                            HorizontalExpand="True">
                <ScrollContainer VerticalExpand="True"
                                 HorizontalExpand="True"
                                 HScrollEnabled="False">
                    <BoxContainer Orientation="Vertical"
                                  Name="Notes"
                                  Access="Public"
                                  VerticalExpand="True"
                                  SeparationOverride="4" />
                </ScrollContainer>
            </PanelContainer>
            <Button Name="ShowMoreButton"
                    Text="{Loc admin-notes-show-more}"
                    Visible="False"
                    HorizontalAlignment="Center"
                    StyleClasses="DS14MenuProfileControl" />
            <Button Name="NewNoteButton"
                    Access="Public"
                    Text="{Loc admin-notes-new-note}"
                    Disabled="True"
                    StyleClasses="DS14MenuProfileControl" />
        </BoxContainer>
    </PanelContainer>
    <!-- DS14-end -->
</Control>
